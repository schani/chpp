Changes since version 0.3.4:

  * bugfixes

  * new macros: listSelect, fglob, timeUNIX, timeUNIX2Hash,
    timeHash2UNIX, timeNow, eval, hdelete

  * a let special form (similar to Lisp's let*)

  * macros for manipulation of environments

  * a defspecial special form for defining special forms :-)

Changes since version 0.3.3:

  * bugfixes

  * updated documentation

  * w3lib

Changes since version 0.3.2:

  * bugfixes

  * new macros: srange, smap, return, flock, scrypt

  * no documentation :-(((

Changes since version 0.3.1:

  * bugfixes

  * new macros: fgetwd, fchdir

  * macros changes: fpipe, fopen

  * varargs are now documented (see define)

  * lambda is documented

  * configuration now happens through autoconf
